  <!-- Mobile Section Title -->
  {%- if section.settings.title != blank -%}
    <div class="mobile-section-title text-center mb-8">
      <h2 class="text-2xl md:text-3xl font-light leading-tight">
        {{ section.settings.title }}
      </h2>
    </div>
  {%- endif -%}

  <!-- Desktop Section Title -->
  {%- if section.settings.title != blank -%}
    <div class="desktop-section-title text-center mb-12 w-full">
      <h2 class="text-3xl md:text-4xl font-light leading-tight">
        {{ section.settings.title }}
      </h2>
    </div>
  {%- endif -%}
