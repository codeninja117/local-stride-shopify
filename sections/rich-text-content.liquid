{% comment %}
  Rich Text Content Section
{% endcomment %}

{% style %}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{% endstyle %}

<div class="section-{{ section.id }}-padding gradient color-{{ section.settings.color_scheme }}">
  <div class="page-width">
    {%- if section.settings.rich_text != blank -%}
      <div class="prose prose-lg mx-auto max-w-none" style="max-width: {{ section.settings.content_max_width }}px;">
        {{ section.settings.rich_text }}
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Rich Text Content",
  "class": "section-rich-text-content js-section-color",
  "tag": "section",
  "settings": [
    {
      "type": "richtext",
      "id": "rich_text",
      "label": "Rich Text Content",
      "default": "<p>Add your content here...</p>"
    },
    {
      "type": "range",
      "id": "content_max_width",
      "min": 400,
      "max": 1200,
      "step": 50,
      "unit": "px",
      "label": "Content max width",
      "default": 800
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "Rich Text Content",
      "category": "Uncategorized",
      "settings": {
        "rich_text": "<p>If you're dealing with that brutal morning heel pain, you're not alone. Over 2 million Americans suffer from plantar fasciitis every year - and are trapped paying $400-800 for custom orthotics that barely work.</p><p>But smart patients are discovering StrideSoles: German-engineered custom orthotics that deliver the same results as expensive clinic versions, for just $199.</p><h2>Here's why they're making the switch:</h2><h3>1. No More 'Walking on Broken Glass' Every Morning</h3>"
      }
    }
  ]
}
{% endschema %}
